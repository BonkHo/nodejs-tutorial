const num1 = 1;
const num2 = 2;

function addValues() {
	console.log(`The sum is: ${num1 + num2}`);
}

addValues();

// require('./8-mind-grenade.js') will automatically execute the code in the file.
// imported modules are automatically invoked (exectued)
